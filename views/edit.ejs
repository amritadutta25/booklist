<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <%- include("./partials/nav.ejs") %>

    <h1>Editing <%= book.title %></h1>

    <form action="/books/<%= book._id %>/?_method=PUT" method="post">
        Title: <input type="text" name="title" value="<%= book.title %>">
        Author: <input type="text" name="author" value="<%= book.author %>">

        <!-- 'checked' is a property of type="checkbox" to tick mark the checkbox -->
        Completed: <input 
        type="checkbox" 
        name="completed"
        <% if(book.completed=== true){ %> 
            checked 
        <% } %>
        >
        <input type="submit" value="Update Book">
    
    </form>
</body>
</html>